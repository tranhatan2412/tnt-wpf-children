<Window x:Class="tnt_wpf_children.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:tnt_wpf_children.Views"
        xmlns:local="clr-namespace:tnt_wpf_children"
        xmlns:vm="clr-namespace:tnt_wpf_children.ViewModels"
        Style="{StaticResource MaterialDesignWindow}"
        Height="720" Width="1080" WindowStartupLocation="CenterScreen" AllowsTransparency="True"
        Background="Transparent" WindowStyle="None" Loaded="Window_Loaded">
    
    

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:RelativeViewModel}">
            <views:Relative />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SessionViewModel}">
            <views:Session />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StatisticsViewModel}">
            <views:Statistics />
        </DataTemplate>
    </Window.Resources>

    <Border CornerRadius="20" Background="#EFF2F7" 
            MouseDown="Border_MouseDown" >
        
        
        
        
        <DockPanel>
           
            
            

            <Grid Width="200" DockPanel.Dock="Left">
                

                

                <Border Background="#623ED0" CornerRadius="20 0 0 20">
                    <DockPanel>
                        
                        

                        <TextBlock Text="Aeon Mall Kids" Foreground="White" FontSize="22" FontWeight="SemiBold" DockPanel.Dock="Top" Margin="5 25 0 10" TextAlignment="Center"/>

                        <Separator Margin="25" Background="#7E5FDC" DockPanel.Dock="Top" Height="0.8"/>

                        <StackPanel DockPanel.Dock="Top" >
                            <Button Style="{StaticResource menuButton}" Command="{Binding GoToSessionCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="BabyCarriage" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Phiên gửi trẻ"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource menuButton}" Command="{Binding GoToRelativeCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="HumanMaleChild" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Người gửi trẻ"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource menuButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileWord" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Báo cáo"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource menuButton}" Command="{Binding GoToStatisticsCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ChartBar" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Thống kê"/>
                                </StackPanel>
                            </Button>

                            

                        </StackPanel>

                        <Button Style="{StaticResource menuButton}" VerticalAlignment="Bottom" DockPanel.Dock="Bottom" Margin="15 0 15 20"
                                Command="{Binding LogoutCommand}"
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Logout" Style="{StaticResource menuButtonIcon}"/>
                                <TextBlock Text="Logout"/>
                            </StackPanel>
                        </Button>

                    </DockPanel>
                </Border>

            </Grid>

            <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Right" Margin="10" Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
                        <Setter Property="Width" Value="30"/>
                        <Setter Property="Height" Value="30"/>
                    </Style>
                </StackPanel.Resources>
                <Button Foreground="Gray" Click="Minimize_Click">
                    <materialDesign:PackIcon Kind="WindowMinimize" />
                </Button>
                <Button Foreground="Gray" Click="Maximize_Click">
                    <materialDesign:PackIcon x:Name="MaximizeIcon" Kind="WindowMaximize" />
                </Button>
                <Button Foreground="Red" Click="Close_Click">
                    <materialDesign:PackIcon Kind="CloseCircle" />
                </Button>
            </StackPanel>

            <Grid Margin="30 0 20 0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    
                </Grid.RowDefinitions>

                <ContentControl Content="{Binding CurrentViewModel}" Grid.Row="1" />

            </Grid>

        </DockPanel>
    </Border>
</Window>
