<Window x:Class="tnt_wpf_children.Views.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tnt_wpf_children"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        FontSize="18" FontFamily="{DynamicResource MaterialDesignFont}" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent" 
        Height="400" Width="600"
        Title="Đăng nhập">
    <Grid Margin="20" MouseDown="Grid_MouseDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1.22*"/>
        </Grid.ColumnDefinitions>


        <Border CornerRadius="10 0 0 10" ClipToBounds="True">
            <Border.Background>
                <ImageBrush ImageSource="/Image/children.jpg" AlignmentX="Center" AlignmentY="Center" Stretch="UniformToFill"/>
            </Border.Background>
            <Grid>

                <StackPanel Margin="0 120 0 0" VerticalAlignment="Top">
                    <TextBlock  FontSize="46" FontWeight="Bold" Foreground="Black" TextAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>


        <Border Grid.Column="1" Background="#FFF5FCFF" CornerRadius="0 10 10 0" >
            <Grid>
                <StackPanel HorizontalAlignment="Right" Margin="0,5,5,0" Orientation="Horizontal" VerticalAlignment="Top">
                    <StackPanel.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
                            <Setter Property="Width" Value="30"/>
                            <Setter Property="Height" Value="30"/>
                        </Style>
                    </StackPanel.Resources>
                    <Button ToolTip="Thu nhỏ" Foreground="Gray" Click="MinimizeLogin_Click">
                        <materialDesign:PackIcon Kind="WindowMinimize" />
                    </Button>
                    <Button ToolTip="Thoát" Foreground="Red" Click="CloseLogin_Click">
                        <materialDesign:PackIcon Kind="CloseCircle" />
                    </Button>
                </StackPanel>

                <StackPanel VerticalAlignment="Center" Margin="70 0">
                    <TextBox BorderThickness="0 0 0 1.5" Padding="0 10" BorderBrush="#C5C8CC"  materialDesign:HintAssist.FloatingOffset="0,-16" materialDesign:HintAssist.Hint="Tên đăng nhập" materialDesign:HintAssist.HintOpacity="0.4" Style="{StaticResource MaterialDesignFloatingHintTextBox}" MaxLength="20" />

                    <PasswordBox Margin="0 20 0 20" Padding="0 10" Background="Transparent" BorderThickness="0 0 0 1.5" BorderBrush="#C5C8CC" materialDesign:HintAssist.FloatingOffset="0,-16" materialDesign:HintAssist.Hint="Mật khẩu" materialDesign:HintAssist.HintOpacity="0.4" materialDesign:TextFieldAssist.HasClearButton="True" Style="{StaticResource MaterialDesignFilledRevealPasswordBox}" MaxLength="20" />
                    <StackPanel Margin="0 30 0 0">
                        <Button HorizontalAlignment="Center" Content="Đăng nhập" FontSize="20" Height="45" Width="130" Style="{StaticResource MaterialDesignPaperLightButton}"></Button>
                    </StackPanel>
                </StackPanel>
                <WrapPanel Margin="20" VerticalAlignment="Bottom" HorizontalAlignment="Center" Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" FontSize="12">Chưa có tài khoản?</TextBlock>
                    <Button FontSize="12" Content="Đăng ký" Click="SignUp_Click" Padding="5 0" Margin="10 0" Style="{StaticResource MaterialDesignPaperLightButton}"/>
                </WrapPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
