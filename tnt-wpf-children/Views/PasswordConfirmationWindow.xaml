<Window x:Class="tnt_wpf_children.Views.PasswordConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Xác thực" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border CornerRadius="10" Background="White" BorderBrush="#DDDDDD" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="XÁC THỰC QUẢN TRỊ VIÊN" 
                       HorizontalAlignment="Center" FontWeight="Bold" FontSize="18" 
                       Foreground="#1976D2" Margin="0,0,0,30"/>

            <StackPanel Grid.Row="2">
                <PasswordBox x:Name="PasswordBox"
                             Padding="0 10" Background="Transparent" BorderThickness="0 0 0 1.5" BorderBrush="#C5C8CC" 
                        materialDesign:HintAssist.FloatingOffset="0,-16" 
                        materialDesign:HintAssist.Hint="Nhập mật khẩu quản trị viên"
                        materialDesign:HintAssist.HintOpacity="0.4" 
                        materialDesign:TextFieldAssist.HasClearButton="True" 
                        Style="{StaticResource MaterialDesignFilledRevealPasswordBox}" MaxLength="20"
                             Margin="0,0,0,20"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,20,0,0">
                <Button Content="HỦY" Foreground="White"
                        Command="{Binding CancelCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedLightButton}"
                        materialDesign:ButtonAssist.CornerRadius="5"
                        Margin="0,0,10,0" Width="100"/>
                
                <Button Content="XÁC NHẬN" 
                        Command="{Binding ConfirmCommand}"
                        CommandParameter="{Binding ElementName=PasswordBox}"
                        Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                        materialDesign:ButtonAssist.CornerRadius="5"
                        Background="#1976D2" Foreground="White"
                        Width="110"/>
            </StackPanel>

            <materialDesign:Snackbar MessageQueue="{Binding MessageQueue}" Grid.RowSpan="4" VerticalAlignment="Bottom" />
        </Grid>
    </Border>
</Window>
