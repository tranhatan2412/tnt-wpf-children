<UserControl x:Class="tnt_wpf_children.Views.Relative"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:tnt_wpf_children"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.DataGrid.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <local:RowNumberConverter x:Key="RowNumberConverter"/>
        </ResourceDictionary>
        
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>


            <TextBlock
       Text="Danh sách trẻ em"
       FontSize="32"
       FontWeight="SemiBold"
       Margin="0 20 0 0"
       Foreground="#121518"/>


            <StackPanel Grid.Column="1"
        Orientation="Horizontal"
        VerticalAlignment="Center"
        Margin="0 20 0 0">


                <Button Style="{StaticResource addButton}" >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountMultiplePlus"
                                 Width="20" Margin="0 0 5 0"
                                 Height="20" VerticalAlignment="Center"
                                 />
                        <TextBlock VerticalAlignment="Center" Text="Thêm mới"/>
                    </StackPanel>
                </Button>


                <Button Style="{StaticResource gridSaveButton}"
        Margin="8 0 0 0" Width="32" Height="32"
        >
                    <materialDesign:PackIcon Kind="ContentSaveEdit" Width="20" Height="20" ToolTip="Lưu thay đổi"
                             Style="{StaticResource gridButtonIcon}" />
                </Button>

            </StackPanel>
        </Grid>




        <Separator Grid.Row="1" Margin="0 20 0 0" Background="#D0D0D0" Height="1"/>

        <FrameworkElement x:Name="DataContextProxy" DataContext="{Binding}" />
        <DataGrid Grid.Row="2" AlternationCount="1000000"
    VerticalGridLinesBrush="#D0D0D0" GridLinesVisibility="All"
    AutoGenerateColumns="False" RowHeight="40"
    CanUserAddRows="False" ScrollViewer.VerticalScrollBarVisibility="Auto"
    HeadersVisibility="All"
    EnableRowVirtualization="True" EnableColumnVirtualization="True" VirtualizingPanel.ScrollUnit="Item"
    VirtualizingPanel.IsVirtualizing="True"
    VirtualizingPanel.VirtualizationMode="Recycling"
    ItemsSource="{Binding Items1}"
    SelectionUnit="{Binding ElementName=selectionUnitComboBox, Path=SelectedValue}">

            <DataGrid.Columns>
                <DataGridCheckBoxColumn
            Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
            CanUserResize="False">
                    <DataGridCheckBoxColumn.Header>
                        <Border Background="Transparent">
                            <CheckBox
                        IsChecked="{Binding DataContext.IsAllItems1Selected, Source={x:Reference DataContextProxy}}" />
                        </Border>
                    </DataGridCheckBoxColumn.Header>
                    <DataGridCheckBoxColumn.HeaderStyle>
                        <Style TargetType="{x:Type DataGridColumnHeader}" 
                       BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                        </Style>
                    </DataGridCheckBoxColumn.HeaderStyle>
                </DataGridCheckBoxColumn>



                <DataGridTextColumn Header="#" Width="Auto" CanUserResize="False"
            CanUserSort="False" IsReadOnly="True"
                                    HeaderStyle="{StaticResource CenterDataGridHeaderStyle}">
                    <DataGridTextColumn.Binding>
                        <Binding RelativeSource="{RelativeSource AncestorType=DataGridRow}" 
                    Path="AlternationIndex" 
                    Converter="{StaticResource RowNumberConverter}" />
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>



                <DataGridTextColumn 
            HeaderStyle="{StaticResource CenterDataGridHeaderStyle}"
            Header="Họ và tên" Width="Auto" Binding="{Binding FullName,
           Mode=TwoWay,
                              UpdateSourceTrigger=PropertyChanged,
                              ValidatesOnDataErrors=True,
                              NotifyOnValidationError=True }" >
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Họ và tên" HorizontalAlignment="Center"/>
                                <TextBox Width="Auto" Margin="0,4,0,0"
                            Text="{Binding DataContext.NameFilter,
                            RelativeSource={RelativeSource AncestorType=DataGrid},
                            UpdateSourceTrigger=PropertyChanged}"
                            materialDesign:HintAssist.Hint="Nhập họ và tên"            />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox"
BasedOn="{StaticResource DataGridTextBoxStyle}">

                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>



                <DataGridTextColumn CanUserSort="False"
                            HeaderStyle="{StaticResource CenterDataGridHeaderStyle}"
                            Header="Số điện thoại" Width="Auto"
                            Binding="{Binding PhoneNumber, Mode=TwoWay,
                              UpdateSourceTrigger=PropertyChanged,
                              ValidatesOnDataErrors=True}" >
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Số điện thoại" HorizontalAlignment="Center"/>
                                <TextBox Width="Auto" Margin="0,4,0,0"
                            Text="{Binding DataContext.PhoneFilter, RelativeSource={RelativeSource AncestorType=DataGrid},
                            UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Nhập SĐT"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox"
               BasedOn="{StaticResource DataGridTextBoxStyle}">
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>

                </DataGridTextColumn>


                <DataGridTextColumn
            HeaderStyle="{StaticResource CenterDataGridHeaderStyle}"
            Header="Ngày tạo" Width="Auto" IsReadOnly="True" Binding="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy hh:mm:ss tt}}" >
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Ngày tạo" HorizontalAlignment="Center"/>

                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <DatePicker Width="Auto"
                                SelectedDate="{Binding DataContext.CreatedFrom,
                                RelativeSource={RelativeSource AncestorType=DataGrid},
                                UpdateSourceTrigger=PropertyChanged}"/>

                                    <TextBlock VerticalAlignment="Center" Text="-" Margin="5,0"/>

                                    <DatePicker Width="Auto"
                                SelectedDate="{Binding DataContext.CreatedTo,
                                RelativeSource={RelativeSource AncestorType=DataGrid},
                                UpdateSourceTrigger=PropertyChanged}">
                                       
                                    </DatePicker>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>


                <DataGridTextColumn 
            HeaderStyle="{StaticResource CenterDataGridHeaderStyle}"
            Header="Ngày cập nhật" Width="Auto" IsReadOnly="True" Binding="{Binding UpdatedAt, StringFormat={}{0:dd/MM/yyyy hh:mm:ss tt}}" >
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Ngày cập nhật" HorizontalAlignment="Center"/>

                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,4,0,0">
                                    <DatePicker Width="Auto"
                                SelectedDate="{Binding DataContext.UpdatedFrom,
                                RelativeSource={RelativeSource AncestorType=DataGrid},
                                UpdateSourceTrigger=PropertyChanged}"/>

                                    <TextBlock VerticalAlignment="Center" Text="-" Margin="5,0"/>

                                    <DatePicker Width="Auto"
                                SelectedDate="{Binding DataContext.UpdatedTo,
                                RelativeSource={RelativeSource AncestorType=DataGrid},
                                UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Width="Auto " CanUserResize="False" IsReadOnly="True">
                    <DataGridTemplateColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource CenterDataGridHeaderStyle}">
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                        </Style>
                    </DataGridTemplateColumn.HeaderStyle>
                    <DataGridTemplateColumn.Header>
                        <Button Width="30" Height="30" Padding="0" Style="{StaticResource gridRemoveButton}" ToolTip="Xóa bộ lọc">
                            <materialDesign:PackIcon Width="20" Height="20" Kind="FilterRemove" Style="{StaticResource gridButtonIcon}" />
                        </Button>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

                                <Button Style="{StaticResource gridRemoveButton}">
                                    <materialDesign:PackIcon Kind="Delete" Style="{StaticResource gridButtonIcon}" />
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
