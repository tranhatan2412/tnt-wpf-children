<UserControl x:Class="tnt_wpf_children.Views.Relative"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:tnt_wpf_children.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="Danh sách người thân" FontSize="32" FontWeight="SemiBold" Margin="0 20 0 0" Foreground="#121518" HorizontalAlignment="Left"/>

            
            <Button Style="{StaticResource addButton}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0 1 8 0" Width="11" Height="11"/>
                    <TextBlock Text="Thêm mới"/>
                </StackPanel>
            </Button>
        </StackPanel>




        <Separator Grid.Row="1" Margin="0 20 0 0" Background="#D0D0D0" Height="1"/>

        <FrameworkElement x:Name="DataContextProxy" DataContext="{Binding}" />
        <DataGrid Grid.Row="2" AlternationCount="1000000"
    VerticalGridLinesBrush="#D0D0D0" GridLinesVisibility="All"
    AutoGenerateColumns="False"
    CanUserAddRows="False" ScrollViewer.VerticalScrollBarVisibility="Auto"
    HeadersVisibility="All"
    EnableRowVirtualization="True" EnableColumnVirtualization="True" VirtualizingPanel.ScrollUnit="Pixel"
    VirtualizingPanel.IsVirtualizing="True"
    VirtualizingPanel.VirtualizationMode="Recycling"
    ItemsSource="{Binding Items1}"
    SelectionUnit="{Binding ElementName=selectionUnitComboBox, Path=SelectedValue}">

            <DataGrid.Columns>
                <DataGridCheckBoxColumn
            Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
            CanUserResize="False">
                    <DataGridCheckBoxColumn.Header>
                        <Border Background="Transparent">
                            <CheckBox
                        IsChecked="{Binding DataContext.IsAllItems1Selected, Source={x:Reference DataContextProxy}}" />
                        </Border>
                    </DataGridCheckBoxColumn.Header>
                    <DataGridCheckBoxColumn.HeaderStyle>
                        <Style TargetType="{x:Type DataGridColumnHeader}" 
                       BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                        </Style>
                    </DataGridCheckBoxColumn.HeaderStyle>
                </DataGridCheckBoxColumn>



                <DataGridTextColumn Header="#" Width="Auto" CanUserResize="False"
            CanUserSort="False" IsReadOnly="True">
                    <DataGridTextColumn.Binding>
                        <Binding RelativeSource="{RelativeSource AncestorType=DataGridRow}" 
                    Path="AlternationIndex" 
                    Converter="{StaticResource RowNumberConverter}" />
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>



                <DataGridTextColumn IsReadOnly="True"
            HeaderStyle="{StaticResource CenterDataGridHeaderStyle}"
            Header="Họ và tên" Width="*" Binding="{Binding FullName,
            UpdateSourceTrigger=PropertyChanged
            }" >
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Họ và tên" HorizontalAlignment="Center"/>
                                <TextBox Width="Auto" Margin="0,4,0,0"
                            Text="{Binding DataContext.NameFilter,
                            RelativeSource={RelativeSource AncestorType=DataGrid},
                            UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>



                <DataGridTextColumn CanUserSort="False" IsReadOnly="True"
                            HeaderStyle="{StaticResource CenterDataGridHeaderStyle}"
                            Header="Số điện thoại" Width="*"
                            Binding="{Binding PhoneNumber,
                            UpdateSourceTrigger=PropertyChanged}" >
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Số điện thoại" HorizontalAlignment="Center"/>
                                <TextBox Width="Auto" Margin="0,4,0,0"
                            Text="{Binding DataContext.PhoneFilter, RelativeSource={RelativeSource AncestorType=DataGrid},
                            UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>


                <DataGridTextColumn 
            HeaderStyle="{StaticResource CenterDataGridHeaderStyle}"
            Header="Ngày tạo" Width="*" IsReadOnly="True" Binding="{Binding CreatedAt}" >
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Ngày tạo" HorizontalAlignment="Center"/>

                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <DatePicker Width="Auto"
                                SelectedDate="{Binding DataContext.CreatedFrom,
                                RelativeSource={RelativeSource AncestorType=DataGrid},
                                UpdateSourceTrigger=PropertyChanged}"/>

                                    <TextBlock VerticalAlignment="Center" Text="-" Margin="5,0"/>

                                    <DatePicker Width="Auto"
                                SelectedDate="{Binding DataContext.CreatedTo,
                                RelativeSource={RelativeSource AncestorType=DataGrid},
                                UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>


                <DataGridTextColumn 
            HeaderStyle="{StaticResource CenterDataGridHeaderStyle}"
            Header="Ngày cập nhật" Width="*" IsReadOnly="True" Binding="{Binding UpdatedAt}" >
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Ngày cập nhật" HorizontalAlignment="Center"/>

                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,4,0,0">
                                    <DatePicker Width="Auto"
                                SelectedDate="{Binding DataContext.UpdatedFrom,
                                RelativeSource={RelativeSource AncestorType=DataGrid},
                                UpdateSourceTrigger=PropertyChanged}"/>

                                    <TextBlock VerticalAlignment="Center" Text="-" Margin="5,0"/>

                                    <DatePicker Width="Auto"
                                SelectedDate="{Binding DataContext.UpdatedTo,
                                RelativeSource={RelativeSource AncestorType=DataGrid},
                                UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Action buttons -->
                <DataGridTemplateColumn Width="Auto" CanUserResize="False" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource gridEditButton}">
                                    <materialDesign:PackIcon Kind="Pencil" Style="{StaticResource gridButtonIcon}" />
                                </Button>
                                <Button Margin="5,0,0,0" Style="{StaticResource gridRemoveButton}">
                                    <materialDesign:PackIcon Kind="Delete" Style="{StaticResource gridButtonIcon}" />
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
