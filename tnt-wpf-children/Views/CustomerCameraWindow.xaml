<Window x:Class="tnt_wpf_children.Views.CustomerCameraWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:cam="clr-namespace:tnt_wpf_children.Views"
        Title="Khách hàng" Height="450" Width="600"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border CornerRadius="10" Background="#000000" ClipToBounds="True">
        <Grid MouseDown="Grid_MouseDown">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" MouseDown="Grid_MouseDown">
                <StackPanel HorizontalAlignment="Right" Margin="0,0,5,0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Foreground="Gray" Click="Minimize_Click" Width="30" Height="30" Padding="0" Background="Transparent" BorderThickness="0">
                        <materialDesign:PackIcon Kind="WindowMinimize" Width="20" Height="20"/>
                    </Button>
                    <Button Foreground="Red" Click="Close_Click" Width="30" Height="30" Padding="0" Background="Transparent" BorderThickness="0">
                        <materialDesign:PackIcon Kind="CloseCircle" Width="20" Height="20"/>
                    </Button>
                </StackPanel>
            </Border>

            <cam:CameraPanel Grid.Row="1" x:Name="CameraPreview"/>
            
        </Grid>
    </Border>
</Window>
